---
name: module-demand
description: 需求整理阶段使用的通用模版。根据接口说明、前端需求、原型图、截图或 Figma 链接，生成 docs/modules/<feature>.md 需求卡，不写业务代码。
---

# 需求整理

## 适用时机

- 用户给了后端接口文档、接口样例、字段说明或联调口径，需要整理成前端需求卡。
- 用户给了简单需求描述、页面效果图、原型截图或 Figma 链接，需要拆成可开发的页面需求。
- 用户给了多个页面或多个入口，需要按页面 / 流程拆分 `docs/modules/<feature>.md`。
- 后续准备进入 `module-kickoff`、`module-scaffold`、`module-api` 前，需要先沉淀需求文档。

## 目标与边界

- 目标是把零散材料整理成可执行的需求卡。
- 只整理需求文档，不写页面代码、不改接口封装、不注册路由。
- 信息不足时先沉淀已知内容，并在需求卡中标明待确认项。

## 输入来源

按用户实际提供的材料取用，不要强行补齐所有项：

- 文字需求：业务目标、入口、按钮、状态、跳转、限制条件。
- 视觉材料：Figma、截图、原型图、标注图；有 Figma 时记录链接，没有则记录图片路径。
- 接口材料：接口路径、请求方法、请求参数、返回字段、状态码、错误文案、登录态依赖。
- 项目上下文：`<project_route_config_file-pages.json>`、相似旧页面、已有 `docs/modules/*.md`。

## 输出文件

- 默认生成或更新 `docs/modules/<feature>.md`。
- `<feature>` 优先使用用户给定的模块名；没有明确名称时，用页面标题或核心业务名命名。
- 多个页面差异明显时，优先拆成多个需求卡，例如 `新手闯关.md`、`新手闯关-视频.md`、`新手闯关-训练.md`。
- 需求卡格式参考 `docs/modules/_TEMPLATE.md`，不要新建额外模板文件。

## 推荐结构

需求卡按以下顺序组织。没有信息的部分保留标题并写明“待确认”，不要编造：

```markdown
## <页面或模块名称>
入口：<从哪个页面、哪个按钮或哪个 tab 进入；待确认则写待确认>

## UI
有 figma 链接优先使用 figma 开发，没有 figma 使用原型图片开发。
如果启动 module-handUI SKILL，必须使用 **figma** 链接开发。

<页面名称>：
figma：<链接或无>
原型图：<相对路径或无>

## 注意
1. <当前需求边界、二级跳转处理、待接入提示、权限限制等>

## 需求
- <页面展示内容、状态、交互、跳转、弹窗、空态、加载态>

## 接口
- <接口名称 / 方法 / 路径 / 入参 / 返回 / 依赖登录态 / 待确认项>
```

## 执行步骤

1. 先确认用户提供的是单页面、单流程还是多页面模块。
2. 如果已有 `docs/modules/<feature>.md`，先读旧文档，按增量更新，不覆盖无关内容。
3. 读取 `docs/modules/_TEMPLATE.md`，确保输出结构和当前项目需求卡一致。
4. 必要时读取 `<project_route_config_file-pages.json>` 和 1 到 2 个相似页面，只用于确认入口、页面归属和现有命名习惯。
5. 将材料拆成：
   - 页面入口与路由线索
   - UI 来源
   - 页面展示需求
   - 交互 / 跳转 / 弹窗
   - 接口与字段
   - 待确认项
6. 写入或更新需求卡。
7. 最后说明生成了哪些文档、哪些字段或流程仍待确认，以及下一步建议进入哪个模块技能。

## 硬规则

- 只整理需求文档，不写页面代码、不改接口封装、不注册路由。
- 不要凭空猜字段。接口材料没给出字段时，在需求卡里标为“待后端确认”。
- 不要把设计稿里不存在、用户也没描述的功能扩写成开发范围。
- 页面样式类需求要落到具体页面或区块；如果需求过多，按一个页面效果一张需求卡拆分。
- 二级跳转页面如果不在本次需求内，要在“注意”里写清楚，例如“点击后弹框提示 `待接入`，不新增页面代码”。
- 需求卡里的图片路径优先写相对仓库路径，例如 `<relative_image_path-./images/xxx.png>` 或 `<docs_image_path-docs/images/xxx.png>`。
- 输出格式参考 `docs/modules/_TEMPLATE.md`，路径必须写对，不使用错误拼写路径。
- 如果用户提供 Figma 链接，只记录到需求卡；本技能不做设计稿回填，后续交给 `module-handUI`。
- 接口内容按原文保留 `method`、`url`、字段名、枚举值和示例 JSON，不翻译字段名。

## 拆分规则

- 单入口单页面：写一个 `docs/modules/<feature>.md`。
- 同一模块下多个明确页面：按页面拆分，主入口保留总览，子页面单独成文档。
- 同一页面包含列表、详情、弹窗、提交结果等复杂流程：优先写在同一需求卡内，用小节区分；只有成为独立页面时再拆文件。
- 接口文档很长但前端只用其中一部分：需求卡只摘当前页面需要的接口，并保留原接口文档路径或来源说明。

## 待确认项写法

遇到信息不足时，不要停在口头提问；先把可确定内容写进文档，并在对应位置列出待确认：

- `入口待确认`
- `路由归属待确认`
- `接口路径待确认`
- `字段含义待后端确认`
- `空态 / 错误态文案待产品确认`
- `二级页面是否本期开发待确认`

## 不负责的内容

- 页面开发、页面壳子、样式回填、接口接入、联调修复。
- 修改 `<project_route_config_file-pages.json>`、`<project_request_file-utils/request.js>`、`<project_api_config_file-config/api.js>` 或业务页面。
- 根据不完整接口材料自行实现 mock 字段或前端字段映射。

## 收尾建议

- 给出已新增 / 更新的需求卡路径。
- 简要列出需求卡覆盖范围和待确认项。
- 下一步通常进入 `module-kickoff`；如果用户已经明确要开发，再按模块工作流继续。
