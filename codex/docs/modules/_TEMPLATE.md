# `<需求名称>`

入口：`<页面 / 按钮 / tab / 路由>`

## UI

- figma：`<链接 / 无>`
- 原型图：`<相对路径 / 无>`

### 页面开发 / 回填

- 有 figma 链接优先使用 figma 开发，没有 figma 使用原型图片开发。
- 如果启动 `module-handUI` SKILL，必须使用 **figma** 链接开发。

### 资源下载

- 如果启动 `figma-asset-triage` SKILL：
  1. 如果用户给出了自定义资源路径，则使用自定义资源路径导出图片。
  2. 如果用户没有给出自定义资源路径，则使用 **figma** 链接判断图片是否导出，并导出。
- 自定义资源路径：`<无 / 路径>`

## 图片

- 静态图片引用路径：`<例如 @/assets/images/training/step/***.png>`
- 占位图片：`@/assets/images/delete.png`
- 素材目录：`<例如 ~/Desktop/workspace/sxtd/sdxq-static/images/training>`
- 参考图：`<可选，填写相对路径>`

## 注意

1. 只完成当前需求卡内容；如果有二级跳转页面，在函数中弹框提示 `待接入`，不要写页面代码。
2. `<其他边界 / 限制 / 待确认事项>`

## 需求

### 1. `<页面 / 模块名称>`

- `<展示内容>`
- `<交互行为>`
- `<状态 / 空态 / 异常态>`
- `<跳转 / 弹窗 / 权限限制>`

### 2. `<可选子模块>`

- `<...>`

## 接口

### 1. `<接口名称>`

- 方法：`<GET / POST / PUT / DELETE>`
- 路径：`<接口路径>`
- 用途：`<接口用途>`
- 入参：`<字段 / 说明>`
- 返回：`<字段 / 说明>`
- 备注：`<待后端确认 / 依赖登录态 / 字段说明>`

## 待确认

- `<入口待确认>`
- `<路由归属待确认>`
- `<接口路径待确认>`
- `<字段含义待后端确认>`
- `<二级页面是否本期开发待确认>`
